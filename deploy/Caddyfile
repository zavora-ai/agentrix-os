# TLS reverse proxy for Agentrix OS.
# Set DOMAIN in deploy/.env (e.g. os.zavora.example).

{$DOMAIN} {
	encode zstd gzip

	@voice path /ws/*
	handle @voice {
		reverse_proxy app:9847
	}

	reverse_proxy app:9847
}