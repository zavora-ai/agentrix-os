{
  "name": "spatial-os",
  "version": "1.0.0",
  "description": "",
  "main": "capture.js",
  "scripts": {
    "capture": "node capture.js",
    "capture:prod": "node -e \"if(!process.env.AGENTRIX_URL)throw new Error('set AGENTRIX_URL')\" && node capture.js",
    "smoke:mobile": "node smoke-mobile.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0",
    "puppeteer-core": "^23.11.1"
  }
}
